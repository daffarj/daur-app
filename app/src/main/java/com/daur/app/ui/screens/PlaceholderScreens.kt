package com.daur.app.ui.screens

import androidx.compose.foundation.layout.*
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Alignment
import androidx.compose.ui.Modifier
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp
import com.daur.app.ui.theme.OnSurfaceVariant

// Placeholder — akan diganti satu per satu

@Composable
fun SetorSampahScreen() {
    Box(Modifier.fillMaxSize(), contentAlignment = Alignment.Center) {
        Text("Setor Sampah — Coming Soon", fontSize = 16.sp, fontWeight = FontWeight.Medium, color = OnSurfaceVariant)
    }
}

@Composable
fun RiwayatSetoranScreen() {
    Box(Modifier.fillMaxSize(), contentAlignment = Alignment.Center) {
        Text("Riwayat Setoran — Coming Soon", fontSize = 16.sp, fontWeight = FontWeight.Medium, color = OnSurfaceVariant)
    }
}

@Composable
fun TukarPoinScreen() {
    Box(Modifier.fillMaxSize(), contentAlignment = Alignment.Center) {
        Text("Tukar Poin — Coming Soon", fontSize = 16.sp, fontWeight = FontWeight.Medium, color = OnSurfaceVariant)
    }
}

@Composable
fun ProfilScreen() {
    Box(Modifier.fillMaxSize(), contentAlignment = Alignment.Center) {
        Text("Profil — Coming Soon", fontSize = 16.sp, fontWeight = FontWeight.Medium, color = OnSurfaceVariant)
    }
}

@Composable
fun KatalogSampahScreen() {
    Box(Modifier.fillMaxSize(), contentAlignment = Alignment.Center) {
        Text("Katalog Sampah — Coming Soon", fontSize = 16.sp, fontWeight = FontWeight.Medium, color = OnSurfaceVariant)
    }
}

@Composable
fun EdukasiLingkunganScreen() {
    Box(Modifier.fillMaxSize(), contentAlignment = Alignment.Center) {
        Text("Edukasi Lingkungan — Coming Soon", fontSize = 16.sp, fontWeight = FontWeight.Medium, color = OnSurfaceVariant)
    }
}
